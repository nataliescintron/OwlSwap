#Keeps this folder recognized as a Python package for Flask imports.
#This file helps Python and Flask
# recognize the folder as a package so imports between routes, models, and app files work correctly.
#Even if it’s empty, it helps avoid import issues and keeps the project structure organized.